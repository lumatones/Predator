# ── Dependencies ──
node_modules/
.pnp
.pnp.js

# ── Build output ──
dist/
dist-electron/
release/
release-installer/
*.tsbuildinfo

# ── IDE & Editor ──
.vscode/
.idea/
*.swp
*.swo
*~
.notes.md

# ── OS files ──
.DS_Store
Thumbs.db
desktop.ini
*.log
*.lnk
*.zip

# ── External binaries (dev tools, not part of project) ──
freebuff.exe
Freebuff-*.exe
rg.exe
tree-sitter.wasm
_nul

# ── User artifacts ──
release_info.json
*release_info.json

# ── Promo video (separate project) ──
promo/

# ── Temp dirs ──
.remotion-skill-temp/

# ── Environment ──
.env
.env.local
.env.*.local

# ── Freebuff Desktop ──
.freebuff/

# ── Installer build cache ──
installer/node_modules/
installer/release/

# ── TypeScript output (from tsconfig.node.json) ──
ts-out/

# ── Temporary files ──
release_info.json
nul
*.tmp

# ── Runtime artifacts ──
.predator_tlsh_db.json

# ── External projects ──
Eye-Animation-Enhancer/

# ── Git credentials ──
.git-credentials
.gitconfig.local
