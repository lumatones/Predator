# ── Server ──
PORT=3001

# ── MySQL (WAMP) ──
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=predator

# ── JWT (admin auth) ──
JWT_SECRET=predator-super-secret-key-change-in-production
JWT_EXPIRES_IN=24h

# ── Admin default credentials (first setup) ──
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
